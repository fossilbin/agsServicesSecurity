	require([
	    "dojo/_base/window", "dojo/parser",
	    "dojo/request","dojo/query","dojo/domReady!"
	], function(aspect, win, request, query){
//		request.post("mainframeServlet").then(
//		        function(text){
//		            reportList = JSON.parse(text); 
//		            frames = dojo.query("iframe");
//		            for(var i=0;i<frames.length;i++){
//		            	if(reportList[i]) reportChange(frames[i].id, reportList[i]);
//		            }
//		        }
//		);
		}
	);